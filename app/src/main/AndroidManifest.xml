<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".ActividadUF4_1Application"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.UF4TareaMicroCuentos"
        tools:targetApi="31">
        <activity
            android:name=".LeeCuentoActivity"
            android:exported="false"
            android:label="@string/title_activity_lee_cuento"
            />
        <activity
            android:name=".EligeCuentoActivity"
            android:exported="false"
            android:label="@string/title_activity_elige_cuento"
            />
        <activity
            android:name=".InicioActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>